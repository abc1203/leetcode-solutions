// Substrings
string s = "abcde";
cout << s.subtr(2, 2) << endl; // starts from index 2 & copies 2 chars

// Concat
string first = "jason";
string last = "chen";
string full = first + last;
cout << full << endl;

// To convert from string to int, run stoi(s)
// can get char by calling s[i]
// s.length(), s.size()
